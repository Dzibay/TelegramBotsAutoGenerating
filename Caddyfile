# HTTPS + reverse proxy → frontend (nginx внутри Docker)
# Домен должен указывать на IP сервера (A-запись). Порты 80/443 открыты в firewall.

{
	email {$ACME_EMAIL:admin@tg-bots-generate.ru}
}

{$SITE_DOMAIN:localhost}, www.{$SITE_DOMAIN:localhost} {
	reverse_proxy frontend:80
}
